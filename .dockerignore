# Arquivos sensíveis e de desenvolvimento não precisam entrar na imagem Docker
.env
.env.example
.github/
docker-compose.yml
.gitignore
*.md

# Certificados — montados como volume em tempo de execução (não incluir na imagem)
certs/

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
